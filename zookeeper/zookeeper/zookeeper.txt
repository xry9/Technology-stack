如果，哪天发现你的zookeeper少于一半的机器挂了，可以这样解决：把data目录下除了myid的其它文件删除，然后重新启动，如果发现还是启不来，那就把zookeeper服务先停掉，再启动
如果某个follower发现自己的版本和leader版本有冲突（如，旧的leader死去活来时，可能产生），会回滚操作，保持和leander一致。
也存在，还没告诉客户端生效，但提议确实生效的可能
三个方法可以注册watcher,getDate getChildred exits
client,连接一个server(follower/leader/observer),发起一个提议即要做数据更新（创建、删除节点或更新节点数据），server首先要连接到leader（如果server本身就是leader
就不用了），然后leader发起投票问下面follower是否同意这个提议（对应一个唯一的zxid），follower接收到这个提议时首先对比一下这个提议的zxid与自己的手中最高的zxid，
如果比自己的大，肯定要同意并更改数据，如果小于或者等于就拒绝
如果选leader时，各个server之间竞争，如果发起者zxid比自己高，肯定会选发起者，zxid相同也会选
zookeeper号称两个半数以上，一、半数以上机器活着就能对外提供服务，二、半数以上机器同意提议，此提议就生效

